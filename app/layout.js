export const metadata = {
  title: "Solar Solution Calculator",
  description: "Solar Calculator"
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
